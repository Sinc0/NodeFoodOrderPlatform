<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>

    <%- include('../snippets/html_nav_menu_admin.ejs') %>

    <main>
        <!-- <h1>My Products</h1>
        <p> List of all the products...</p> -->
        <% if (orders.length > 0) { %>
        <div class="grid">
        <% for (let order of orders) { %>
            <article class="card product-item">
                <p><%= order._id %> | <%= order.user %> | <%= order.totalPrice %> <button><a href="/admin/delete-order/<%= order._id %> ">X</a></button></p>
                <!--
                <% for (elementCounter = 0; elementCounter < order.products.length; elementCounter++) { %>
                    <p><%= order.products[elementCounter].title %></p>
                    <p><%= order.products[elementCounter].quantity %></p>
                    <p><%= order.products[elementCounter].price %></p>
                    <p><%= order.products[elementCounter].description %></p>
                <% } %>
                -->
            </article>
        <% } %>
    </div>
    <% } else { %>
        <h1> No products found</h1>
    <% } %>
    </main>
</body>
</html>