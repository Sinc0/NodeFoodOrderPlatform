<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/restaurant.css">
    <script src="/js/functionality.js"></script>
    <script src="/js/shortcuts.js"></script>
    <script>
        shortcut.add("escape", function() {document.getElementById("temp").click()});  
    </script>
</head>

<body onload="loadSideCart()">
    <!-- admin view -->
    <% if(admin == true) { %>
        <%- include('../snippets/html_nav_menu_admin.ejs') %>
    
    <!-- logged in user view -->
    <% } else if(loggedIn == true) { %>
        <%- include('../snippets/html_nav_menu_logged_in.ejs') %>
    
    <!-- anonmymous view -->
    <% } else { %>
        <%- include('../snippets/html_nav_menu_anon.ejs') %>
    <% } %>

    <modal>
        <div hidden id="informAnonModal">
            <button id="temp" onclick="InformAnonClose()" style="margin-left: 437.5px;">X</button>

            <br />
            <br />
            
            <p>You need to register or login to be able to order</p>
            
            <br />

            <button><a href="/login">Login</a></button>        
            <button><a href="/register">Register</a></button>
            
            <p></p>

            <br />
            <br />
        </div>
    </modal>

    <main id="main-menu" class="centered">
        <div id="menu">
            <div>
                <h1><%=restaurant.title %></h1>
            </div>
            <!-- <h1><%= restaurant.title %></h1> -->
            <hr>

            <% if(restaurant.open == false) { %>
                <div id="restaurantDetailRestaurantIsClosed">
                    <h1>Closed</h1>
                </div>
            <% } %>

            <div class="card__image">
                <img src="/<%= restaurant.imageUrl %>">
            </div>

            <br />

            <div>
            <!-- <p><%= restaurant._id %></p> -->
            <% for(let c = 0; c < restaurant.hours.length; c++) { %>
                <p><%= restaurant.hours[c] %></p>
            <% } %>
            <!-- <h2><%= restaurant.price %></h2> -->
            </div>
            
            <br />
            
            <div>
            <p><%= restaurant.description %></p>
            <!-- <p><%= restaurant.description %></p> -->
            </div>
            
            <br />
            
            <hr>
            <% for(let c = 0; c < restaurant.menuCategories.length; c++) { %>
                <% let restaurantCategory = restaurant.menuCategories[c] %>
                <div>
                <h2 id="menu-restaurant-title"><%= restaurantCategory.charAt(0).toUpperCase() + restaurantCategory.slice(1) %></h2>
                </div>
                
                <div>
                <% for(let c = 0; c < restaurant.menu.length; c++) { %>
                
                    <% if(restaurant.menu[c].category == restaurantCategory) { %>
                    <div id="menu-item">
                        <!-- <p><%= restaurant.menu[c].restaurantId %></p> -->
                        <div id="menu-item-texts">
                            <p><b><%= restaurant.menu[c].title %></b></p>
                            <p>$<%= restaurant.menu[c].price %></p>
                            <p>Ingredients: <%= restaurant.menu[c].description %></p>
                        </div>
                        <% if(loggedIn == true) { %>
                        <div id="menu-item-add-to-cart-button">
                            <% let buttonNumber = c %>
                            <button onclick="updateCart('<%=buttonNumber %>', '<%=restaurant.menu[c].title %>', '<%=restaurant.menu[c].price %>')" class="cart-add-to-cart-button">+</button></a>
                            <!-- <a href="/addToCart/<%= restaurant._id %>&<%= restaurant.menu[c].restaurantId %>" class="btn"><button id="cart-add-to-cart-button">+</button></a> -->
                        </div>
                        <% } %>

                        <% if(loggedIn != true) { %>
                            <% if(restaurant.open == true) { %>
                                <div id="menu-item-add-to-cart-button">
                                    <% let buttonNumber = c %>
                                    <button onclick="InformAnonOpen()" class="cart-add-to-cart-button">+</button></a>
                                    <!-- <a href="/addToCart/<%= restaurant._id %>&<%= restaurant.menu[c].restaurantId %>" class="btn"><button id="cart-add-to-cart-button">+</button></a> -->
                                </div>
                            <% } %>

                            <% if(restaurant.open == false) { %>
                                <div disabled id="menu-item-add-to-cart-button">
                                    <% let buttonNumber = c %>
                                    <button disabled onclick="InformAnonOpen()" class="cart-add-to-cart-button" style="opacity: .50; visibility: hidden;">+</button></a>
                                    <!-- <a href="/addToCart/<%= restaurant._id %>&<%= restaurant.menu[c].restaurantId %>" class="btn"><button id="cart-add-to-cart-button">+</button></a> -->
                                </div>
                            <% } %>

                        <% } %>
                    </div>
                    <% } else { %>
                    <% } %>
                
                <% } %>
                </div>
                <br />
                
                <hr>
            <% } %>
        </div>
 
        <% if(loggedIn == true) { %>
        <div id="side-cart">
            <form action="/checkout" method="POST">
                <button id="order-button" type="submit">Checkout</button>
                
                <div id="cart-items"></div> 
                
                <div class="">
                    <h2 id="total-price"></h2>
                </div>
                
                <input hidden id="cartAllItems" name="cartAllItems"></input>
                <input hidden id="cartTotalPrice" name="cartTotalPrice"></input>
                <input hidden name="restaurant" value='<%=restaurantUrl %>'></input>
                <input id="customerComment" name="customerComment" value="" maxlength="255" placeholder="Comment to Restaurant" onfocus="this.placeholder=''" onblur="this.placeholder='Comment to Restaurant'"></input>
            </form>
        </div>
        <% } %>
    </main>
</body>
</html>